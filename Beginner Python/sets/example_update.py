# Create a set
my_set = {1, 2, 3}

# Define a list of new elements to add to the set
new_elements = [4, 5, 6]

# Use the update() function to add the new elements to the set
my_set.update(new_elements)

# Print the updated set
print(my_set)
